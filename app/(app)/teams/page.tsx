import { StagePlaceholder } from "@/components/layout/StagePlaceholder";

export default function TeamsPage() {
  return (
    <StagePlaceholder
      title="My Teams"
      subtitle="Track your project teams, roles and skill coverage."
      stage="Stage 7 · Teams"
    />
  );
}
