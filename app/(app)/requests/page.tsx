import { StagePlaceholder } from "@/components/layout/StagePlaceholder";

export default function RequestsPage() {
  return (
    <StagePlaceholder
      title="Requests"
      subtitle="Team invitations you received and sent."
      stage="Stage 8 · Requests"
    />
  );
}
