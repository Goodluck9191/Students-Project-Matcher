import { StagePlaceholder } from "@/components/layout/StagePlaceholder";

export default function NotificationsPage() {
  return (
    <StagePlaceholder
      title="Notifications"
      subtitle="Invitations, accepted requests and new matches."
      stage="Stage 8 · Notifications"
    />
  );
}
