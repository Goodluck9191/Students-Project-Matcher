import { StagePlaceholder } from "@/components/layout/StagePlaceholder";

export default function ProjectsPage() {
  return (
    <StagePlaceholder
      title="Projects"
      subtitle="Search, filter and discover academic projects — or create your own."
      stage="Stage 5 · Projects"
    />
  );
}
